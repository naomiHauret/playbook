import { main } from './server'

/**
 * Entrypoint.
 * Backend for Playbook
 * includes:
 *
 * - REST API
 * - MRU routes
 * - XMTP messaging
 *
 * @see `/src/server` for server related code
 * @see `/src/stackr` for micro-rollup related code
 */

main()
